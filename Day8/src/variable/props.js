// @flow
import type {Props} from '../types/Props';
let props: Props = {
  ToDoList: [
    {
      text: 'Do This',
      isDone: false,
    },
    {
      text: 'Do That',
      isDone: true,
    },
  ],
};

export default props;
